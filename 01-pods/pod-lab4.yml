apiVersion: v1
kind: Pod
metadata:
  name: lab4-pod 
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        limits:
          memory: "200Mi"
          cpu: "500m"
        requests:
          memory: "100Mi"
          cpu: "250m"